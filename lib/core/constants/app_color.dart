import 'package:flutter/material.dart';

class AppColor {
  AppColor._(); // Private constructor to prevent instantiation
  
  static const Color primary = Color(0xFF093731);
  static const Color greenLight = Color(0xFFE8F5F2);
  // Warna untuk button navigation (muted teal)
  static const Color navButtonBg = Color(0xFF2F5C56);
  // Warna untuk inactive dot (light grey)
  static const Color activeDot = Color(0xFF179778);
  static const Color inactiveDot = Color(0xFFFFFFFF);
  static const Color buttonBg = Color(0x00E8F5F2);
}

